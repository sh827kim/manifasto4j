plugins {
    id 'java-library'
}

dependencies {
    implementation project(':manifesto-runtime')
    api project(':manifesto-core')
    api project(':manifesto-host')
    api project(':manifesto-world')
}
