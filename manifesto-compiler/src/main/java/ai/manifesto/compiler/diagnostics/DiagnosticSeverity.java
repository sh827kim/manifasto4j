package ai.manifesto.compiler.diagnostics;

/**
 * KR: DiagnosticSeverity는 컴파일러 진단 계층에서 사용하는 diagnostic severity 분류 값을 열거합니다.
 * EN: DiagnosticSeverity enumerates diagnostic severity classification values used in the compiler diagnostics layer.
 */
public enum DiagnosticSeverity {
    ERROR,
    WARNING
}
