package ai.manifesto.compiler;

/**
 * KR: CompileDomainOptions는 실행 동작을 제어하는 옵션 값을 묶는 설정 객체입니다.
 * EN: CompileDomainOptions is a configuration object bundling options that control runtime behavior.
 */
public record CompileDomainOptions(String fnTableVersion) {}
