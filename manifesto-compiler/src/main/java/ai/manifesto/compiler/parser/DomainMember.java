package ai.manifesto.compiler.parser;

/**
 * KR: DomainMember는 컴파일러 파서 계층에서 domain member 계약을 정의하는 인터페이스입니다.
 * EN: DomainMember is an interface defining the domain member contract in the compiler parser layer.
 */
public interface DomainMember extends AstNode {
}
