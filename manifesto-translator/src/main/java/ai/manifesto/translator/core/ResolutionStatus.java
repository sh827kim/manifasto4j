package ai.manifesto.translator.core;

/**
 * KR: intent graph 노드 해석 상태입니다.
 * EN: Resolution status of an intent graph node.
 */
public enum ResolutionStatus {
    RESOLVED,
    PARTIAL,
    ABSTRACT
}
