dependencies {
    implementation project(':manifesto-core')
    implementation project(':manifesto-host')
    implementation project(':manifesto-world')
    testImplementation project(':manifesto-bridge')
}
