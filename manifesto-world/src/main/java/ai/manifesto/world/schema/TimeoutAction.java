package ai.manifesto.world.schema;

/**
 * KR: TimeoutAction는 World 스키마 계층에서 사용하는 timeout action 분류 값을 열거합니다.
 * EN: TimeoutAction enumerates timeout action classification values used in the World schema layer.
 */
public enum TimeoutAction {
    APPROVE,
    REJECT
}
