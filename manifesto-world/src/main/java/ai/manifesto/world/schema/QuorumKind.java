package ai.manifesto.world.schema;

/**
 * KR: QuorumKind는 World 도메인 상태/분류 체계를 정의하는 열거형입니다.
 * EN: QuorumKind is an enum defining World-domain status or classification categories.
 */
public enum QuorumKind {
    UNANIMOUS,
    MAJORITY,
    THRESHOLD
}
